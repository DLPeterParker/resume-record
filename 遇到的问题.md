冒烟测试通过：GUI 正常构造 → 加载截图 → 后台 OCR+LLM 跑完 → 字段正确填充（公司=滴滴、base=北京市、岗位=Agentic Engineering 研发工程师、进度=投递、日期=2026-08-16），状态栏提示"识别完成，请核对字段后点击确认写入"。

你手动运行

.venv\Scripts\activate
python app_gui.py

或直接 uv run --no-sync python app_gui.py（不会动环境）。


 ❯ 本来昨天配置的环境可行，可以运行，结果今天重新运行他就自动uv更新环境了，怎么禁止这种行为？之后不会再发生了吧？

● 我先查清楚 uv 到底为什么会自动更新环境，再给你确定的解决办法。



 uv run / uv sync 在没有 uv.lock 锁文件时，每次运行都会重新解析依赖到最新版。你的代码是按 PaddleOCR 2.x
  写的，但最新版是 3.x，所以昨天好好的 2.x 环境，今天一跑就被 uv 悄悄升成了 3.x、崩了。

  我做的三件事（已钉死，不会再自动变）

1. 生成了 uv.lock——把 paddleocr==2.8.1、paddlepaddle==2.6.2 以及所有传递依赖全部锁死。之后 uv run/uv sync  会照锁文件来，不再去抓最新版。
2. pyproject.toml 加了 [tool.uv] package = false——声明这是脚本项目，uv 只管理依赖、不打包。
3. 加了 .python-version = 3.10——固定解释器版本，换机器也不会漂移。





  之后怎么跑（任选其一，都不会再动环境）

**方式一（最推荐，和 README 一致，完全不经过 uv）**

```
  .venv\Scripts\activate
  python main.py --batch ./简历投递记录/
```

**方式二：跳过同步，直接用现有环境**

```
  uv run --no-sync python main.py --batch ./简历投递记录/
```

**方式三：冻结锁文件，永不更新依赖**

```
  uv run --frozen python main.py --batch ./简历投递记录/
```

  我已经验证 uv run --no-sync 跑出来就是 paddleocr 2.8.1 / paddle 2.6.2，没问题。

  回答你的核心问题

  之后不会再自动更新了，因为现在有了 uv.lock。唯一还会变版本的情况是：有人手动改 pyproject.toml 里的版本号，或显式执行
  uv lock --upgrade / uv sync --upgrade。日常 uv run / uv sync 不会再升到 3.x 了。