[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "resume-record"
version = "1.0.0"
description = "基于 OCR 与大模型（LLM）的简历投递记录自动归档工具"
readme = "README.md"
requires-python = ">=3.10,<3.11"
dependencies = [
    "paddlepaddle==2.6.2",
    "paddleocr==2.8.1",
    "opencv-python==4.11.0.86",
    "setuptools>=68",
    "openpyxl>=3.1.0",
    "python-dotenv>=1.0.0",
    "openai>=1.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
]

[tool.uv]
# 扁平脚本项目，不需要把本项目作为包安装，只管理依赖
package = false

# opencv-python 与 opencv-contrib-python 会互相覆盖 cv2 模块导致损坏，
# 这里排除 paddleocr 传递依赖的 opencv-contrib-python，只保留 opencv-python。
override-dependencies = [
    "opencv-contrib-python; sys_platform == 'never'",
]

[tool.setuptools]
# 项目为扁平脚本结构，无需要打包的 Python 包
packages = []

[tool.pytest.ini_options]
testpaths = ["tests"]
